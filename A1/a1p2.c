/*
CH-230-A
a1_p2.c
Aditya Ojha
ad.ojha@jacobs-university.de
*/
#include <stdio.h>
int main() {
 int result; /* The result of our calculation */
result = (2 + 7) * 9 / 3;
printf("The result is %d\n", result );
return 0;
}
/* The program at first prints “The result is -1073745604” or some other
   random numbers because it was not specified of what result to print 
   after printf function to be filled at the gap of "%d". So the computer
    prints a random number */
